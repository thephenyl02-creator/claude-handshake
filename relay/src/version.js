// Frozen constants. Bump RELAY_VERSION on every deploy-visible change; the
// CLI's doctor command and the invite self-check read it from GET /health.
export const RELAY_VERSION = '0.1.0';
export const PROTOCOL_VERSION = 1;
export const SERVICE_NAME = 'claude-handshake-relay';
